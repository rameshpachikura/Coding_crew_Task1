int findNthTerm(int N) {
        int sum=1;
       if(N==1)
       {
           return 1;
       }
       else
       {
          for(int i=2;i<=N;i++)
          {
              sum+=i;
          }
       }
       return sum;
    }
